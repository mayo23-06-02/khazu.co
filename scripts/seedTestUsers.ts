/**
 * Legacy Mongo seed script — retired after Supabase migration.
 * Create users via /auth/register or the Supabase Auth dashboard.
 */
console.log(
  "seedTestUsers is deprecated. Register via /auth/register or Supabase Auth.",
);
